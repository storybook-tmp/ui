{/* Transcript renderer copied directly from ~/code/github/mcp/eval/templates/result-docs/transcript.tsx and transcript.types.ts */}
import {
  emptyTranscriptText,
  hasTranscript,
  transcriptProps,
} from './eval-data.ts';
import { Transcript } from './transcript.tsx';

# Transcript

{!hasTranscript ? (
  <blockquote>{emptyTranscriptText}</blockquote>
) : (
  <>
    {transcriptProps.prompt ? (
      <details>
        <summary>
          Full prompt
          {transcriptProps.promptTokenCount > 0
            ? ` (${transcriptProps.promptTokenCount.toLocaleString()} tokens)`
            : ""}
        </summary>
        <pre>
          <code>{transcriptProps.prompt}</code>
        </pre>
      </details>
    ) : null}

    <Transcript {...transcriptProps} prompt="" />
  </>
)}
