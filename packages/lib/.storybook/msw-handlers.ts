export const mswHandlers = {
  // No network-dependent components in the current story set.
  // Add handlers here as stories exercise API endpoints.
  // Example:
  // products: [http.get('/api/products', () => HttpResponse.json([]))]
};
